<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="0"
    android:versionName="0.0">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MovieApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/filmograf_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/filmograf_round_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filmograf"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".presentation.MoviesActivity"
            android:exported="true"
            android:label="@string/title_activity_movies"
            android:theme="@style/Theme.Filmograf">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>